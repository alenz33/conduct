planned behaviour

pre build:
 * boostrap
 * prebuild check (all bs)

build:
 * per bs
  * check
  * run

postbuild
 * all bs - reversed order
  * cleanup
 * potential rollback (boostrap)


--

TODO:
 * Referencer: give step instead of chain - would allow auto eval
